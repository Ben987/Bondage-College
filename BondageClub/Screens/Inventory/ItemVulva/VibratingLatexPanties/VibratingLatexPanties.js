"use strict";

// Loads the item extension properties
function InventoryItemVulvaVibratingLatexPantiesLoad() {
	if (DialogFocusItem.Property == null) DialogFocusItem.Property = { Intensity: -1 };
	if (DialogFocusItem.Property.Intensity == null) DialogFocusItem.Property.Intensity = -1;
}

// Draw the item extension screen
function InventoryItemVulvaVibratingLatexPantiesDraw() {
}

// Catches the item extension clicks
function InventoryItemVulvaVibratingLatexPantiesClick() {
}